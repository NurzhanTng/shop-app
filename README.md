# 🛍️ Интернет-магазин (Laravel 11 + Vue 3)

Полнофункциональный интернет-магазин с административной панелью, разработанный в качестве тестового задания.

## Примеры UI

- [Каталог товаров](photos/home.png)
- [Фильтр товаров](photos/categories.png)
- [Авторизация](photos/login.png)
- [Заказы пользователя](photos/my_orders.png)
- [Страница админа](photos/admin_panel.png)
- [Все заказы пользователей](photos/all_orders.png)
- [Изменить товар](photos/edit_product.png)

---

## 📝 Архитектурные решения и обоснование

### Выбор технологического стека

**Backend (Laravel 11 + PHP 8.3)**:

- Выбрал сервисный слой вместо Actions для инкапсуляции бизнес-логики, так как он более гибкий для расширения и позволяет легко добавлять новые методы без создания множества классов
- REST API с Laravel Sanctum в режиме Bearer tokens для простоты развертывания и тестирования (не требует настройки cookies/session для SPA)
- Form Requests для валидации на уровне входных данных - это стандарт Laravel для разделения ответственности
- Jobs/Queues использовал для имитации отправки email уведомлений при создании заказа (через log driver, как требовалось)

**Frontend (Vue 3 + TypeScript)**:

- TypeScript для типобезопасности и лучшего developer experience - это одна из "дополнительных возможностей", которая реально ускоряет разработку
- Pinia вместо Vuex, так как это официальное решение для Vue 3 с более простым API
- Composition API для всех компонентов - современный подход с лучшей переиспользуемостью логики
- Tailwind CSS для быстрой стилизации без написания кастомного CSS

### Архитектурные компромиссы

**Что упростил ради времени:**

- Не реализовал полноценную отправку email (использую log driver) - в продакшене подключил бы SMTP/SendGrid
- Изображения товаров - URL из Unsplash вместо реальной загрузки файлов (избежал сложности с storage/filesystem)
- Нет пагинации на фронтенде для списка товаров (API поддерживает, но UI упрощен)
- Базовый UI без продвинутых анимаций - сфокусировался на функциональности и архитектуре

**Что реализовал из дополнительных требований:**

1. ✅ **TypeScript для Vue.js** - полная типизация всех компонентов, stores и API
2. ✅ **Pinia для state management** - 4 stores (auth, cart, products, orders) с TypeScript
3. ✅ **Docker Compose** - полная настройка с 4 сервисами (db, backend, queue, frontend), запуск одной командой
4. ✅ **Загрузка изображений товаров** - через URL (упрощенный вариант)

### Избежание N+1 проблемы

Во всех сервисах использую eager loading:

- `Product::with('category')` - при получении товаров сразу загружаем категории
- `Order::with(['user', 'items.product'])` - при получении заказов загружаем связанные данные
- Это критично для производительности при работе со списками

### Структура кода

**Backend**: Тонкие контроллеры → Сервисы (бизнес-логика) → Модели (данные)

- Контроллеры только принимают запрос и возвращают ответ
- Сервисы содержат всю логику (создание заказа с позициями, расчет total и т.д.)
- Модели описывают структуру данных и relationships

**Frontend**: Компонентный подход с разделением ответственности

- `api/` - HTTP клиенты для взаимодействия с backend
- `stores/` - глобальное состояние приложения
- `components/` - переиспользуемые UI компоненты (12 штук)
- `views/` - страницы приложения (10 страниц)

### Особенности реализации

**Корзина**: Хранится в localStorage (Pinia store с persistence), не требует авторизации для просмотра товаров  
**Роли**: Простая role-based авторизация через middleware `AdminMiddleware`  
**API**: RESTful endpoints с разделением на публичные, защищенные и административные  
**Queue Worker**: Отдельный Docker контейнер для обработки очередей в фоне

---

## 📋 Описание

**Backend**: Laravel 11, PHP 8.3, MySQL, Laravel Sanctum  
**Frontend**: Vue 3, TypeScript, Vite, Tailwind CSS, Pinia  
**Архитектура**: REST API, Service Layer, SPA

### Основные возможности

#### Для покупателей:

- 🔐 Регистрация и авторизация
- 📦 Просмотр каталога товаров
- 🔍 Поиск и фильтрация по категориям
- 🛒 Корзина (сохраняется в localStorage)
- 💳 Оформление заказов
- 📋 Просмотр истории заказов

#### Для администраторов:

- ➕ Управление товарами (CRUD)
- 📂 Управление категориями (CRUD)
- 📊 Просмотр заказов
- ✅ Изменение статусов заказов
- 📈 Статистика по заказам

## 🚀 Быстрый старт

### Требования

- Docker & Docker Compose
- Git

### Установка и запуск

```bash
# 1. Клонировать репозиторий
git clone git@github.com:NurzhanTng/shop-app.git
cd shop

# 2. Запустить проект (одна команда!)
make install
```

Результат

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:8000/api
- **База данных**: localhost:3306

### Тестовые пользователи

После установки доступны следующие аккаунты:

```
Администратор:
Email: admin@example.com
Password: password

Пользователь 1:
Email: john@example.com
Password: password

Пользователь 2:
Email: jane@example.com
Password: password
```

## 📁 Структура проекта

```
shop/
├── backend/              # Laravel 11 API
│   ├── app/
│   │   ├── Http/
│   │   │   ├── Controllers/
│   │   │   ├── Middleware/
│   │   │   └── Requests/
│   │   ├── Models/
│   │   ├── Services/     # Бизнес-логика
│   │   └── Jobs/         # Очереди
│   ├── database/
│   │   ├── migrations/
│   │   ├── seeders/
│   │   └── factories/
│   └── routes/
│       └── api.php
│
├── frontend/             # Vue 3 SPA
│   ├── src/
│   │   ├── api/         # API клиенты
│   │   ├── components/  # Компоненты
│   │   ├── stores/      # Pinia stores
│   │   ├── router/      # Vue Router
│   │   ├── views/       # Страницы
│   │   └── types/       # TypeScript типы
│   └── public/
│
├── docker-compose.yml    # Docker конфигурация
├── Makefile             # Удобные команды
└── README.md
```

## 🛠️ Команды Make

```bash
make help              # Показать все доступные команды
make install           # Первая установка проекта
make up                # Запустить контейнеры
make down              # Остановить контейнеры
make restart           # Перезапустить контейнеры
make logs              # Показать логи всех сервисов
make backend           # Логи backend
make frontend          # Логи frontend
make clean             # Удалить все контейнеры и данные
make rebuild           # Пересобрать контейнеры

# Работа с backend
make shell-backend     # Войти в контейнер backend
make migrate           # Запустить миграции
make migrate-fresh     # Пересоздать БД с тестовыми данными
make composer-install  # Установить PHP зависимости
make test-backend      # Запустить тесты

# Работа с frontend
make shell-frontend    # Войти в контейнер frontend
make npm-install       # Установить Node зависимости
make test-frontend     # Запустить тесты
```

## 🔧 Ручная установка (без Docker)

### Backend

```bash
cd backend

# Установить зависимости
composer install

# Создать .env файл
cp .env.example .env

# Настроить подключение к БД в .env (и создать базу с юзером)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=shop
DB_USERNAME=root
DB_PASSWORD=

# Сгенерировать ключ приложения
php artisan key:generate

# Создать таблицу для очередей
php artisan queue:table

# Запустить миграции с тестовыми данными
php artisan migrate:fresh --seed

# Запустить сервер
php artisan serve

# В другом терминале - запустить обработчик очередей
php artisan queue:work
```

### Frontend

```bash
cd frontend

# Установить зависимости
npm install

# Создать .env файл
cp .env.example .env

# Убедиться что в .env правильный URL API
VITE_API_URL=http://localhost:8000/api

# Запустить dev сервер
npm run dev
```

## 📚 API Endpoints

### Публичные

```
POST   /api/register          # Регистрация
POST   /api/login             # Вход
GET    /api/products          # Список товаров
GET    /api/products/{id}     # Детали товара
GET    /api/categories        # Список категорий
```

### Защищенные (требуют авторизации)

```
POST   /api/logout            # Выход
GET    /api/me                # Текущий пользователь
GET    /api/orders            # Мои заказы
POST   /api/orders            # Создать заказ
GET    /api/orders/{id}       # Детали заказа
```

### Административные (только admin)

```
GET    /api/admin/stats       # Статистика
POST   /api/products          # Создать товар
PUT    /api/products/{id}     # Обновить товар
DELETE /api/products/{id}     # Удалить товар
POST   /api/categories        # Создать категорию
PUT    /api/categories/{id}   # Обновить категорию
DELETE /api/categories/{id}   # Удалить категорию
PATCH  /api/orders/{id}/status # Изменить статус заказа
```

## 🗄️ База данных

### Модели и связи

**User** (Пользователь)

- hasMany → Order

**Category** (Категория)

- hasMany → Product

**Product** (Товар)

- belongsTo → Category
- hasMany → OrderItem

**Order** (Заказ)

- belongsTo → User
- hasMany → OrderItem

**OrderItem** (Позиция заказа)

- belongsTo → Order
- belongsTo → Product

### Seeders

При запуске `php artisan migrate:fresh --seed` создаются:

- 1 администратор
- 2 обычных пользователя
- 3 категории
- 15 товаров с изображениями
- 5 заказов с позициями

## 🎨 Frontend

### Технологии

- **Vue 3** с Composition API
- **TypeScript** для типобезопасности
- **Pinia** для управления состоянием
- **Vue Router** для навигации
- **Axios** для HTTP запросов
- **Tailwind CSS** для стилей

### Основные компоненты

- `ProductCard` - карточка товара
- `ProductList` - список товаров
- `ProductFilters` - фильтры и поиск
- `CartItem` - элемент корзины
- `CartSummary` - итоги корзины
- `OrderCard` - карточка заказа
- `OrderStatusBadge` - статус заказа
- `ProductForm` - форма товара (админ)

### Stores (Pinia)

- `authStore` - авторизация
- `cartStore` - корзина
- `productsStore` - товары
- `ordersStore` - заказы

## 🧪 Тестирование

### Backend

```bash
# Запустить все тесты
php artisan test

# Запустить конкретный тест
php artisan test --filter ProductTest
```

### Frontend

```bash
# Запустить тесты
npm run test

# Запустить с покрытием
npm run test:coverage
```

## 🐛 Отладка

### Просмотр логов

```bash
# Все сервисы
make logs

# Только backend
make backend

# Только frontend
make frontend

# База данных
make db
```

### Очистка кэша Laravel

```bash
docker-compose exec backend php artisan config:clear
docker-compose exec backend php artisan cache:clear
docker-compose exec backend php artisan route:clear
```

### Пересоздание БД

```bash
make migrate-fresh
# или
docker-compose exec backend php artisan migrate:fresh --seed
```

---

**Сделано с ❤️ используя Laravel 11 и Vue 3**
